{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "emcc",
            "type": "shell",
            "command": "cd emsdk && ./emsdk install latest && ./emsdk activate latest && source ./emsdk_env.sh"
        },
        {
            "label": "compile",
            "type": "shell",
            "command": "make"
        }
    ]
}